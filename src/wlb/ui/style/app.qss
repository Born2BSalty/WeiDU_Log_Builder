/* Global UI theme tweaks live here for easy adjustments. */

QWidget {
    font-size: 12pt;
}

QLabel#PageTitle {
    font-size: 18pt;
    font-weight: 600;
}

QLabel#PageSubtitle {
    font-size: 11pt;
    color: #5a5a5a;
}

QPushButton {
    padding: 6px 12px;
}

QWidget#ScanToolbar {
    background: transparent;
    border: none;
}
